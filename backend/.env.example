# Server Configuration
PORT=3001
NODE_ENV=development
JWT_SECRET=your-jwt-secret-key-here

# Database Configuration
DATABASE_URL="mysql://root:password@localhost:3306/atode?schema=public"

# 本番環境用の設定例
# DATABASE_URL="mysql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?connection_limit=5"

# CORS Configuration
FRONTEND_URL=http://localhost:3000
